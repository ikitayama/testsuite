IF (DEFINED M_gnu_xx)
SET (MUTATEE_bgq_gxx64dyn ON CACHE STRING "Build mutatees: compiler bgq_g++, ABI 64-bit, dyn linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_bgq_gxx64dyn OFF CACHE STRING "Build mutatees: compiler bgq_g++, ABI 64-bit, dyn linked")
ENDIF()
IF (DEFINED M_gnu_xx)
SET (MUTATEE_bgq_gxx64stat ON CACHE STRING "Build mutatees: compiler bgq_g++, ABI 64-bit, stat linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_bgq_gxx64stat OFF CACHE STRING "Build mutatees: compiler bgq_g++, ABI 64-bit, stat linked")
ENDIF()
IF (DEFINED M_gnu_cc)
SET (MUTATEE_bgq_gcc64dyn ON CACHE STRING "Build mutatees: compiler bgq_gcc, ABI 64-bit, dyn linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_bgq_gcc64dyn OFF CACHE STRING "Build mutatees: compiler bgq_gcc, ABI 64-bit, dyn linked")
ENDIF()
IF (DEFINED M_gnu_cc)
SET (MUTATEE_bgq_gcc64stat ON CACHE STRING "Build mutatees: compiler bgq_gcc, ABI 64-bit, stat linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_bgq_gcc64stat OFF CACHE STRING "Build mutatees: compiler bgq_gcc, ABI 64-bit, stat linked")
ENDIF()
IF (DEFINED M_bg_cc)
SET (MUTATEE_bgxlc64dyn ON CACHE STRING "Build mutatees: compiler bgxlc, ABI 64-bit, dyn linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_bgxlc64dyn OFF CACHE STRING "Build mutatees: compiler bgxlc, ABI 64-bit, dyn linked")
ENDIF()
IF (DEFINED M_bg_cc)
SET (MUTATEE_bgxlc64stat ON CACHE STRING "Build mutatees: compiler bgxlc, ABI 64-bit, stat linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_bgxlc64stat OFF CACHE STRING "Build mutatees: compiler bgxlc, ABI 64-bit, stat linked")
ENDIF()
IF (DEFINED M_bg_CC)
SET (MUTATEE_bgxlcxx64dyn ON CACHE STRING "Build mutatees: compiler bgxlc++, ABI 64-bit, dyn linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_bgxlcxx64dyn OFF CACHE STRING "Build mutatees: compiler bgxlc++, ABI 64-bit, dyn linked")
ENDIF()
IF (DEFINED M_bg_CC)
SET (MUTATEE_bgxlcxx64stat ON CACHE STRING "Build mutatees: compiler bgxlc++, ABI 64-bit, stat linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_bgxlcxx64stat OFF CACHE STRING "Build mutatees: compiler bgxlc++, ABI 64-bit, stat linked")
ENDIF()
