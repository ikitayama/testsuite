IF (DEFINED M_gnu_cxx)
SET (MUTATEE_gxx32dyn ON CACHE STRING "Build mutatees: compiler g++, ABI 32-bit, dyn linked")
SET (BUILD_LIBS_32 1)
ELSE()
SET (MUTATEE_gxx32dyn OFF CACHE STRING "Build mutatees: compiler g++, ABI 32-bit, dyn linked")
ENDIF()
IF (DEFINED M_gnu_cxx)
SET (MUTATEE_gxx32stat ON CACHE STRING "Build mutatees: compiler g++, ABI 32-bit, stat linked")
SET (BUILD_LIBS_32 1)
ELSE()
SET (MUTATEE_gxx32stat OFF CACHE STRING "Build mutatees: compiler g++, ABI 32-bit, stat linked")
ENDIF()
IF (DEFINED M_gnu_cc)
SET (MUTATEE_gcc32dyn ON CACHE STRING "Build mutatees: compiler gcc, ABI 32-bit, dyn linked")
SET (BUILD_LIBS_32 1)
ELSE()
SET (MUTATEE_gcc32dyn OFF CACHE STRING "Build mutatees: compiler gcc, ABI 32-bit, dyn linked")
ENDIF()
IF (DEFINED M_gnu_cc)
SET (MUTATEE_gcc32stat ON CACHE STRING "Build mutatees: compiler gcc, ABI 32-bit, stat linked")
SET (BUILD_LIBS_32 1)
ELSE()
SET (MUTATEE_gcc32stat OFF CACHE STRING "Build mutatees: compiler gcc, ABI 32-bit, stat linked")
ENDIF()
