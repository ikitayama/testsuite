IF (DEFINED M_gnu_cc)
SET (MUTATEE_gcc32dyn ON CACHE STRING "Build mutatees: compiler gcc, ABI 32-bit, dyn linked")
SET (BUILD_LIBS_32 1)
ELSE()
SET (MUTATEE_gcc32dyn OFF CACHE STRING "Build mutatees: compiler gcc, ABI 32-bit, dyn linked")
ENDIF()
IF (DEFINED M_gnu_cc)
SET (MUTATEE_gcc32stat ON CACHE STRING "Build mutatees: compiler gcc, ABI 32-bit, stat linked")
SET (BUILD_LIBS_32 1)
ELSE()
SET (MUTATEE_gcc32stat OFF CACHE STRING "Build mutatees: compiler gcc, ABI 32-bit, stat linked")
ENDIF()
IF (DEFINED M_gnu_cc)
SET (MUTATEE_gcc64dyn ON CACHE STRING "Build mutatees: compiler gcc, ABI 64-bit, dyn linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_gcc64dyn OFF CACHE STRING "Build mutatees: compiler gcc, ABI 64-bit, dyn linked")
ENDIF()
IF (DEFINED M_gnu_cc)
SET (MUTATEE_gcc64stat ON CACHE STRING "Build mutatees: compiler gcc, ABI 64-bit, stat linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_gcc64stat OFF CACHE STRING "Build mutatees: compiler gcc, ABI 64-bit, stat linked")
ENDIF()
IF (DEFINED M_intel_CC_32)
SET (MUTATEE_iCC32dyn ON CACHE STRING "Build mutatees: compiler iCC, ABI 32-bit, dyn linked")
SET (BUILD_LIBS_32 1)
ELSE()
SET (MUTATEE_iCC32dyn OFF CACHE STRING "Build mutatees: compiler iCC, ABI 32-bit, dyn linked")
ENDIF()
IF (DEFINED M_intel_CC)
SET (MUTATEE_iCC64dyn ON CACHE STRING "Build mutatees: compiler iCC, ABI 64-bit, dyn linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_iCC64dyn OFF CACHE STRING "Build mutatees: compiler iCC, ABI 64-bit, dyn linked")
ENDIF()
IF (DEFINED M_intel_cc_32)
SET (MUTATEE_icc32dyn ON CACHE STRING "Build mutatees: compiler icc, ABI 32-bit, dyn linked")
SET (BUILD_LIBS_32 1)
ELSE()
SET (MUTATEE_icc32dyn OFF CACHE STRING "Build mutatees: compiler icc, ABI 32-bit, dyn linked")
ENDIF()
IF (DEFINED M_intel_cc)
SET (MUTATEE_icc64dyn ON CACHE STRING "Build mutatees: compiler icc, ABI 64-bit, dyn linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_icc64dyn OFF CACHE STRING "Build mutatees: compiler icc, ABI 64-bit, dyn linked")
ENDIF()
IF (DEFINED M_gnu_cxx)
SET (MUTATEE_gxx32dyn ON CACHE STRING "Build mutatees: compiler g++, ABI 32-bit, dyn linked")
SET (BUILD_LIBS_32 1)
ELSE()
SET (MUTATEE_gxx32dyn OFF CACHE STRING "Build mutatees: compiler g++, ABI 32-bit, dyn linked")
ENDIF()
IF (DEFINED M_gnu_cxx)
SET (MUTATEE_gxx32stat ON CACHE STRING "Build mutatees: compiler g++, ABI 32-bit, stat linked")
SET (BUILD_LIBS_32 1)
ELSE()
SET (MUTATEE_gxx32stat OFF CACHE STRING "Build mutatees: compiler g++, ABI 32-bit, stat linked")
ENDIF()
IF (DEFINED M_gnu_cxx)
SET (MUTATEE_gxx64dyn ON CACHE STRING "Build mutatees: compiler g++, ABI 64-bit, dyn linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_gxx64dyn OFF CACHE STRING "Build mutatees: compiler g++, ABI 64-bit, dyn linked")
ENDIF()
IF (DEFINED M_gnu_cxx)
SET (MUTATEE_gxx64stat ON CACHE STRING "Build mutatees: compiler g++, ABI 64-bit, stat linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_gxx64stat OFF CACHE STRING "Build mutatees: compiler g++, ABI 64-bit, stat linked")
ENDIF()
IF (DEFINED M_pg_cc)
SET (MUTATEE_pgcc32dyn ON CACHE STRING "Build mutatees: compiler pgcc, ABI 32-bit, dyn linked")
SET (BUILD_LIBS_32 1)
ELSE()
SET (MUTATEE_pgcc32dyn OFF CACHE STRING "Build mutatees: compiler pgcc, ABI 32-bit, dyn linked")
ENDIF()
IF (DEFINED M_pg_cc)
SET (MUTATEE_pgcc64dyn ON CACHE STRING "Build mutatees: compiler pgcc, ABI 64-bit, dyn linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_pgcc64dyn OFF CACHE STRING "Build mutatees: compiler pgcc, ABI 64-bit, dyn linked")
ENDIF()
IF (DEFINED M_pg_cxx)
SET (MUTATEE_pgCC32dyn ON CACHE STRING "Build mutatees: compiler pgCC, ABI 32-bit, dyn linked")
SET (BUILD_LIBS_32 1)
ELSE()
SET (MUTATEE_pgCC32dyn OFF CACHE STRING "Build mutatees: compiler pgCC, ABI 32-bit, dyn linked")
ENDIF()
IF (DEFINED M_pg_cxx)
SET (MUTATEE_pgCC64dyn ON CACHE STRING "Build mutatees: compiler pgCC, ABI 64-bit, dyn linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_pgCC64dyn OFF CACHE STRING "Build mutatees: compiler pgCC, ABI 64-bit, dyn linked")
ENDIF()
