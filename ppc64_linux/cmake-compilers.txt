IF (DEFINED M_gnu_cxx)
SET (MUTATEE_gxx64dyn ON CACHE STRING "Build mutatees: compiler g++, ABI 64-bit, dyn linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_gxx64dyn OFF CACHE STRING "Build mutatees: compiler g++, ABI 64-bit, dyn linked")
ENDIF()
IF (DEFINED M_gnu_cxx)
SET (MUTATEE_gxx64stat ON CACHE STRING "Build mutatees: compiler g++, ABI 64-bit, stat linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_gxx64stat OFF CACHE STRING "Build mutatees: compiler g++, ABI 64-bit, stat linked")
ENDIF()
IF (DEFINED M_gnu_cc)
SET (MUTATEE_gcc64dyn ON CACHE STRING "Build mutatees: compiler gcc, ABI 64-bit, dyn linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_gcc64dyn OFF CACHE STRING "Build mutatees: compiler gcc, ABI 64-bit, dyn linked")
ENDIF()
IF (DEFINED M_gnu_cc)
SET (MUTATEE_gcc64stat ON CACHE STRING "Build mutatees: compiler gcc, ABI 64-bit, stat linked")
SET (BUILD_LIBS_64 1)
ELSE()
SET (MUTATEE_gcc64stat OFF CACHE STRING "Build mutatees: compiler gcc, ABI 64-bit, stat linked")
ENDIF()
